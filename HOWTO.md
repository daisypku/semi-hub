# 知识库使用手册

> 这是你自己的随时备查手册，包含：发布报告流程、Markdown 语法、实用贴士、未来扩展方向。

## 目录

1. [发布新报告的标准流程](#1-发布新报告的标准流程)
2. [Markdown 语法速查](#2-markdown-语法速查)
3. [实用小贴士](#3-实用小贴士)
4. [未来可扩展的模块](#4-未来可扩展的模块)
5. [常见问题](#5-常见问题)

---

## 1. 发布新报告的标准流程

### Step A：确定文件名

把 ticker 的点号换成下划线：

| 原 ticker | 文件名 |
|---|---|
| `002463.SZ` | `002463_SZ.md` |
| `9992.HK` | `9992_HK.md` |
| `5706.T` | `5706_T.md` |
| `600183.SS` | `600183_SS.md` |
| `AVGO` | `AVGO.md` |

### Step B：在 GitHub 网页创建文件

1. 打开 `ccl-research-hub` 仓库
2. 右上角 **Add file** → **Create new file**
3. 文件名框里输入**完整路径**（注意斜杠）：
   ```
   company/data/reports/002463_SZ.md
   ```
4. 下方大框粘贴 Markdown 内容
5. 底部 Commit message 随便写，比如 `docs: 沪电股份深度研究 v1`
6. 点绿色 **Commit changes**

### Step C：等 30 秒 ~ 2 分钟

GitHub Pages 自动重新部署。

### Step D：验证

```
https://daisypku.github.io/ccl-research-hub/company/?ticker=002463.SZ
```

### Step E：修改已有报告

回到那个 `.md` 文件页面 → 右上角**铅笔图标**直接编辑 → Commit changes。

---

## 2. Markdown 语法速查

```markdown
# 一级标题（每份报告顶部用一次）
## 二级标题（大章节）
### 三级标题（小节）

**加粗文字**
*斜体*
~~删除线~~

> 引用块 —— 适合放关键数据、警示

- 无序列表项
- 第二项
  - 缩进子项

1. 有序列表
2. 第二条

| 表头 | 表头 | 表头 |
|---|---|---|
| 数据 | 数据 | 数据 |

[链接文字](https://example.com)
[跳到另一家公司](./600183_SS.md)
![图片描述](./images/chart.png)

---（三个横线 = 分隔线）

`行内代码` 适合放 ticker、数字、术语

```python
# 代码块
print("hello")
```
```

---

## 3. 实用小贴士

### 3.1 报告之间可以互相引用

在沪电股份报告里引用生益科技：

```markdown
生益科技（见[生益科技深度](./600183_SS.md)）提供上游 CCL 材料，
两者构成"材料+成品"的完整组合。
```

用相对路径 `./ticker_slug.md` 就能跳转到另一家公司的报告页。

### 3.2 贴图片

在 `company/data/reports/` 下建一个 `images/` 子文件夹，把图片上传进去，然后在 Markdown 里：

```markdown
![营收拆分](./images/002463_revenue_split.png)
```

### 3.3 长报告不要在 GitHub 网页写

GitHub 网页编辑器对超长文档不友好。**超过 2000 字的报告建议在本地写好再上传**：

- 在你电脑上用 VS Code / Typora / Obsidian 写 `.md` 文件
- 写完后 GitHub 仓库主页 → **Add file** → **Upload files** 拖进去

本地写的好处：断网也能写、编辑器功能强、不怕浏览器意外刷新丢草稿。

### 3.4 想更新基础信息（公司介绍那句话）

编辑 `company/data/companies.json`，找到对应 ticker 的 `description` 字段修改。

### 3.5 报告格式完全自由

不需要按任何固定模板。几句话、几条要点、或几千字深度研究都能正常显示。
随着你写作习惯固化，可以慢慢发展出自己的风格。

### 3.6 本地打开 diagram002.html 不能用了

因为新版需要用 JavaScript 加载 JSON/MD 文件，浏览器的安全策略禁止 `file://` 协议下的这种读取。
**一律用在线地址**：`https://daisypku.github.io/ccl-research-hub/diagram002.html`。
把它加到浏览器书签，手机电脑通用。

---

## 4. 未来可扩展的模块

这些不急着做，等需求自然出现再加。积累了 10+ 份报告之后可以考虑：

- **「相关公司」模块**：公司页自动列出同产业链环节的其他公司，方便横向对比
- **总览目录页**：列出所有已有报告的公司，一目了然看进度
- **搜索框**：输入 ticker 或公司名直接跳转
- **财务速览表**：在 JSON 里加 `financials` 字段，模板页自动渲染关键指标表
- **业绩/公告时间线**：JSON 加 `timeline` 数组，按时间轴展示
- **实时股价 Sparkline**：复用 Market Daily 的 yfinance 数据，给每家公司加一张迷你走势图
- **上下游关系图**：可视化展示"谁是谁的客户/供应商"
- **按主题的集合页**：比如"AI PCB 主线"、"防御性股息"各自一个专题入口

需要时告诉我，都可以搭。

---

## 5. 常见问题

### Q：点击公司后页面白屏或显示"无法加载公司数据"
**A**：GitHub Pages 可能还在部署。等 1-2 分钟再刷新。如果持续失败，检查 `company/data/companies.json` 文件是否在正确位置。

### Q：报告文件已经上传，但页面显示"深度报告整理中"
**A**：检查文件名——必须是 `ticker_slug.md`（点号替换为下划线），必须放在 `company/data/reports/` 里。大小写要一致。

### Q：想删除已发布的报告
**A**：在 GitHub 仓库里找到那个 `.md` 文件 → 点进去 → 右上角垃圾桶图标 → Commit changes。下次访问那家公司页就会回到"整理中"状态。

### Q：JSON 文件改动后页面没变
**A**：浏览器可能缓存了旧版。用 `Ctrl + F5` 或 `Ctrl + Shift + R` 强制刷新。

### Q：想把某份报告设为私密、只给特定人看
**A**：GitHub Pages 的公开站点做不到访问控制。如果某份报告不想公开，就**不要上传**到这个 public 仓库，留在你的私有仓库或本地。

### Q：手机上也能看吗？
**A**：能。模板设计是响应式的，手机浏览器直接打开就能看。

---

*最后更新：2026-04-18*
